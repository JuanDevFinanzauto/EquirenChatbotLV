# Save snapshots (persiste cada N segundos si hay cambios)
save 900 1
save 300 10
save 60 10000

# Max memory settings
maxmemory 2gb
maxmemory-policy volatile-lru

# Enable AOF (append only file)
appendonly yes

# Logging y seguridad básica
loglevel notice
